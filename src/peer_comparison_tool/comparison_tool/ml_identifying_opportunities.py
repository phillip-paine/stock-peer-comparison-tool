import pandas as pd
from typing import Dict


def assemble_data():
    datasets: Dict[str, pd.DataFrame] = {'training': pd.DataFrame(), 'validation': pd.DataFrame()}
    return datasets


def fit_opportunity_model():
    return


def run_opportunity_model():
    return
